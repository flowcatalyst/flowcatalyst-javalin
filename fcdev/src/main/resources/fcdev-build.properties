# Filtered by Maven at build time (fcdev/pom.xml). Read by io.flowcatalyst.fcdev.EmbeddedPg
# and io.flowcatalyst.fcdev.Version. Do not edit the values by hand.
embedded.pg.version=${zonky-binaries.version}
vcs.revision=${fcdev.vcs.revision}
# The function-api a scaffolded function compiles against (`fn init`): the reactor version,
# not fcdev's release semver — function-api is built in the same reactor.
function.api.version=${project.version}
