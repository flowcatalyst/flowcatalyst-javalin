<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.flowcatalyst</groupId>
  <artifactId>flowcatalyst-parent</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>FlowCatalyst (Java)</name>
  <description>Java 25 drop-in implementation of the FlowCatalyst platform (port of flowcatalyst-go)</description>

  <modules>
    <module>usecase</module>
    <module>sdk</module>
    <module>server</module>
    <module>fcdev</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <!-- Stay on Jackson 2.x (com.fasterxml namespace); Jackson 3's split packages are not wanted here. -->
    <jackson.version>2.22.2</jackson.version>
    <javalin.version>7.2.3</javalin.version>
    <jooq.version>3.21.7</jooq.version>
    <hikaricp.version>7.1.0</hikaricp.version>
    <flyway.version>13.3.0</flyway.version>
    <postgresql.version>42.7.13</postgresql.version>
    <slf4j.version>2.0.18</slf4j.version>
    <logback.version>1.6.3</logback.version>
    <nimbus-jose-jwt.version>10.9.1</nimbus-jose-jwt.version>
    <oauth2-oidc-sdk.version>11.38.2</oauth2-oidc-sdk.version>
    <webauthn.version>2.9.0</webauthn.version>
    <password4j.version>1.8.4</password4j.version>
    <prometheus.version>1.8.0</prometheus.version>
    <jedis.version>8.0.0</jedis.version>
    <!-- Overridden to false in `sdk`, whose generated sources we cannot fix. -->
    <maven.compiler.failOnWarning>true</maven.compiler.failOnWarning>
    <awssdk.version>2.54.2</awssdk.version>
    <jnats.version>2.26.2</jnats.version>
    <caffeine.version>3.2.4</caffeine.version>
    <zonky.version>2.2.2</zonky.version>
    <zonky-binaries.version>18.4.0</zonky-binaries.version>
    <picocli.version>4.7.7</picocli.version>
    <mcp.version>2.0.1</mcp.version>
    <json-schema-validator.version>3.0.7</json-schema-validator.version>
    <mongodb.version>5.10.0</mongodb.version>
    <mysql.version>26.7.0</mysql.version>
    <angus-mail.version>2.0.5</angus-mail.version>
    <zxing.version>3.5.4</zxing.version>
    <hdrhistogram.version>2.2.2</hdrhistogram.version>
    <junit.version>5.13.4</junit.version>
    <assertj.version>3.27.7</assertj.version>
    <h2.version>2.4.240</h2.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- BOMs -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${awssdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.zonky.test.postgres</groupId>
        <artifactId>embedded-postgres-binaries-bom</artifactId>
        <version>${zonky-binaries.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- In-repo modules -->
      <dependency>
        <groupId>io.flowcatalyst</groupId>
        <artifactId>flowcatalyst-usecase</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.flowcatalyst</groupId>
        <artifactId>flowcatalyst-sdk</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- HTTP -->
      <dependency>
        <groupId>io.javalin</groupId>
        <artifactId>javalin</artifactId>
        <version>${javalin.version}</version>
      </dependency>

      <!-- Database -->
      <dependency>
        <groupId>org.jooq</groupId>
        <artifactId>jooq</artifactId>
        <version>${jooq.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen</artifactId>
        <version>${jooq.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-meta</artifactId>
        <version>${jooq.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zaxxer</groupId>
        <artifactId>HikariCP</artifactId>
        <version>${hikaricp.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-database-postgresql</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>${postgresql.version}</version>
      </dependency>
      <dependency>
        <groupId>io.zonky.test</groupId>
        <artifactId>embedded-postgres</artifactId>
        <version>${zonky.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-sync</artifactId>
        <version>${mongodb.version}</version>
      </dependency>
      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql.version}</version>
      </dependency>

      <!-- Logging -->
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <!-- Auth / crypto -->
      <dependency>
        <groupId>com.nimbusds</groupId>
        <artifactId>nimbus-jose-jwt</artifactId>
        <version>${nimbus-jose-jwt.version}</version>
      </dependency>
      <dependency>
        <groupId>com.nimbusds</groupId>
        <artifactId>oauth2-oidc-sdk</artifactId>
        <version>${oauth2-oidc-sdk.version}</version>
      </dependency>
      <dependency>
        <groupId>com.yubico</groupId>
        <artifactId>webauthn-server-core</artifactId>
        <version>${webauthn.version}</version>
      </dependency>
      <dependency>
        <groupId>com.password4j</groupId>
        <artifactId>password4j</artifactId>
        <version>${password4j.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.zxing</groupId>
        <artifactId>core</artifactId>
        <version>${zxing.version}</version>
      </dependency>

      <!-- Infra -->
      <dependency>
        <groupId>io.prometheus</groupId>
        <artifactId>prometheus-metrics-core</artifactId>
        <version>${prometheus.version}</version>
      </dependency>
      <dependency>
        <groupId>io.prometheus</groupId>
        <artifactId>prometheus-metrics-exposition-formats</artifactId>
        <version>${prometheus.version}</version>
      </dependency>
      <dependency>
        <groupId>org.hdrhistogram</groupId>
        <artifactId>HdrHistogram</artifactId>
        <version>${hdrhistogram.version}</version>
      </dependency>
      <dependency>
        <groupId>redis.clients</groupId>
        <artifactId>jedis</artifactId>
        <version>${jedis.version}</version>
      </dependency>
      <dependency>
        <groupId>io.nats</groupId>
        <artifactId>jnats</artifactId>
        <version>${jnats.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.ben-manes.caffeine</groupId>
        <artifactId>caffeine</artifactId>
        <version>${caffeine.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.angus</groupId>
        <artifactId>angus-mail</artifactId>
        <version>${angus-mail.version}</version>
      </dependency>
      <dependency>
        <groupId>info.picocli</groupId>
        <artifactId>picocli</artifactId>
        <version>${picocli.version}</version>
      </dependency>
      <dependency>
        <groupId>io.modelcontextprotocol.sdk</groupId>
        <artifactId>mcp</artifactId>
        <version>${mcp.version}</version>
      </dependency>
      <dependency>
        <groupId>com.networknt</groupId>
        <artifactId>json-schema-validator</artifactId>
        <version>${json-schema-validator.version}</version>
      </dependency>

      <!-- Test -->
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
      </dependency>
      <dependency>
        <groupId>com.h2database</groupId>
        <artifactId>h2</artifactId>
        <version>${h2.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.15.0</version>
          <configuration>
            <release>25</release>
            <!-- Warnings are errors, per module. A session found three real
                 bugs via IDE inspections that 2000 tests were green on; the
                 compiler's own warnings deserve the same seriousness rather
                 than scrolling past in build output.

                 A property rather than a flag so the `sdk` module can opt
                 out: its openapi-generator output uses a deprecated Jackson
                 API we neither write nor can fix. Narrowing the exemption to
                 that one module keeps it honest everywhere else. -->
            <failOnWarning>${maven.compiler.failOnWarning}</failOnWarning>
            <compilerArgs>
              <!-- -this-escape is disabled because the only offenders are
                   openapi-generator output under target/generated-sources,
                   which we do not write and cannot fix. Everything else is
                   on, and fatal. -->
              <arg>-Xlint:all,-processing,-serial,-preview,-this-escape</arg>
              <arg>-parameters</arg>
              <!-- StructuredTaskScope is a preview API in Java 25 (JEP 505).
                   Note this PINS the runtime to exactly Java 25: a class file
                   compiled with preview enabled refuses to load on any other
                   release, so a JDK upgrade means recompiling, not just
                   swapping the JVM. -preview above suppresses the per-use
                   warning; the pin is the thing to remember, not the noise. -->
              <arg>--enable-preview</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.6</version>
          <configuration>
            <!-- Preview class files only load on a JVM that opts in too. -->
            <argLine>--enable-preview</argLine>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.2</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>3.6.3</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
