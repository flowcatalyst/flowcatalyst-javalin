# Owner rulings, 2026-09-06

Asked in order after Phase 5's verification landed (parity corpus clean,
frontend e2e green, CI written). Each row names the Java work it creates,
the Go work (appended to `docs/go-mirror/2026-09-06-go-fix-list.md`), and
the parity allow-list entry it touches. "Java" work is scheduled in
`docs/port-plan.md` Phase 5b.

| # | Question | Ruling | Java | Go | Allow-list |
|---|---|---|---|---|---|
| 1 | `$schema` on every JSON response | **Let it go** — Java never emits it | none | none | `**/$schema` stays, ruling id `R-2026-09-06 #1` |
| 2 | OpenAPI documents | **Java serves the vendored lockfile verbatim** (`/api/openapi.json`, `.yaml`, `/q/openapi`, the developer BFF platform spec) plus the A-22 route | serve the lockfile | none | the S3 entries retire |
| 3 | Introspection `client_id` | **RFC 7662 `azp`**; Go takes C-Q26 from the auth-rulings patch | none | apply patch | retires with the patch |
| 4 | WebAuthn ceremony option defaults | **Accept the difference**; no non-browser client reads them | none | none | stays, ruling id `#4` |
| 5 | Pagination envelope | **After cutover** | — | — | — |
| 6 | Platform-scoped scheduled jobs for client-scoped callers | **Hidden from client users**, both sides; Go also fixes its cross-client leak | scheduled-job visibility hides `client_id IS NULL` from client-scoped callers | fix the leak | the two `bff*` entries retire |
| 7 | Event-type `clientScoped` on create | **Honour the field** | carry the request value | mirror `entity.go:208` | new entry until Go mirrors; e2e pin flips |
| 8 | INTERNAL identity-provider create | **Go DTO `*bool,omitempty` + re-dump the lockfile**; Java re-vendors it | re-vendor when dumped | DTO + dump | — |
| 9 | Dispatch-job ingest permission | **Go adopts the seeded permission** | none | one line ×2 | — |
| 10a | Batch event ingest with one invalid item | **Partial success with honest per-item results** (Go `ece54fe`; Java the same night) — valid items are written, each item's outcome is reported; **the outbox poller must read the per-item results** (a batch is no longer one outcome) | ingest writes valid items, reports per item; outbox sink/poller handles per-item outcomes | stop reporting SUCCESS for the invalid item | entry retires once both agree |
| 10b | Audit-log `principalId` absent | **Required** — Go `ece54fe` refuses the item per slot (`BAD_REQUEST`, "principalId is required"); Java the same night  — both sides refuse the item without it; lockfile marks it required | refuse at the domain now, schema when the lockfile is re-dumped | DTO required + dump | new entry until Go refuses |
| 11 | `client_credentials`, client without a principal | **400 `unauthorized_client`** on both | change the token endpoint | `token.go:476,485` | new entry until Go changes |
| 12 | Dispatch-job actions (ignore/complete; requeue's breadth) | **Requeue stays total** — receivers are idempotent and a resend of a completed job is legitimate; a separate "requeue completed" action is an option when the cancel/complete routes are added **after cutover** | none now | none now | — |
| 13 | Principal `/{id}/…` sub-routes | **Scope every sub-route like the by-id read, and gate mutations before loading** (no 404-vs-403 oracle) | principal API | mirror | new entries until Go mirrors |
| 14 | Scheduled-job sync `archiveUnlisted` | **Narrowed to the application** | `SyncScheduledJobs` | mirror | none expected (no corpus scenario crosses applications) |
| 15 | Service-account token mint audit | **Audit-only plan on the mint** (actor, account, never the token; a failed audit does not fail the mint) | mint operation | mirror | none (audit rows are not on the wire) |
| 16 | `ServiceAccountCode` vs `app:<code>` | **Reserved `app:` namespace**: the value object admits it, the API create path refuses it | value object + create | mirror the refusal | none |
| 17 | `iam_login_attempts` retention | **Keep as history**; index sizing and archival are separate work | none (note in `auth-retention.md`) | none | — |
| 18 | Brand fallback | **`FlowCatalyst` for both** | done | config endpoint follows | retires with Go |
| 19 | X-06 at the wire (IdP type, `SetProperty` value type, `UpdateConnection` status) | **Reject unknown values with 400**, Go's rule | three parse sites | none | none |
| 20 | Native fcdev; TypeBox | **Native fcdev before cutover**; TypeBox after | fcdev `-Pnative` profile | — | — |
