{
  "description": "Route allow-list for e2e runs against the Rust fc-dev SPA (E2E_SIDE=rust). Replaces the byte-identical SPA sha gate (spaGate.ts) that go/java use against each other — the Rust frontend is a fork (docs/java-parity-plan.md §4.1), so its /index.html can never match Java's byte-for-byte. Instead: 'shared' lists the client routes both SPAs serve (checked reachable — HTTP 200 on GET — before the suite runs, in runRouteAllowlistGate); 'javaOnly' documents routes the Rust SPA does not have yet, so a spec that visits one of them is a known, explained skip rather than a silent or confusing failure. Regenerate by diffing frontend/src/router/index.ts in both repos (java-parity-plan.md §3 L1); this file was seeded 2026-09-15 against flowcatalyst-javalin@646794d3.",
  "shared": [
    "/logout",
    "/auth/login",
    "/auth/forgot-password",
    "/auth/reset-password",
    "/dashboard",
    "/applications",
    "/applications/new",
    "/applications/:id",
    "/clients",
    "/clients/new",
    "/clients/:id",
    "/users",
    "/users/new",
    "/users/:id",
    "/identity/service-accounts",
    "/identity/service-accounts/new",
    "/identity/service-accounts/:id",
    "/authorization/roles",
    "/authorization/roles/:roleName",
    "/authorization/roles/:roleName/edit",
    "/authorization/permissions",
    "/authentication/identity-providers",
    "/authentication/identity-providers/new",
    "/authentication/identity-providers/:id",
    "/authentication/email-domain-mappings",
    "/authentication/email-domain-mappings/new",
    "/authentication/email-domain-mappings/:id",
    "/authentication/oauth-clients",
    "/authentication/oauth-clients/new",
    "/authentication/oauth-clients/:id",
    "/roles",
    "/authentication/domain-idps",
    "/authentication/anchor-domains",
    "/event-types",
    "/event-types/create",
    "/event-types/:id",
    "/event-types/:id/add-schema",
    "/scheduled-jobs",
    "/scheduled-jobs/create",
    "/scheduled-jobs/:id",
    "/scheduled-jobs/:id/instances",
    "/scheduled-jobs/instances/:instanceId",
    "/subscriptions",
    "/subscriptions/new",
    "/subscriptions/:id",
    "/connections",
    "/connections/new",
    "/connections/:id",
    "/dispatch-pools",
    "/dispatch-pools/new",
    "/dispatch-pools/:id",
    "/dispatch-jobs",
    "/events",
    "/events/:id",
    "/platform/cors",
    "/platform/audit-log",
    "/platform/login-attempts",
    "/platform/settings/theme",
    "/platform/debug/events",
    "/platform/debug/dispatch-jobs",
    "/processes",
    "/processes/create",
    "/processes/:id",
    "/processes/:id/edit",
    "/developer",
    "/developer/applications/:id",
    "/developer/applications/:id/versions",
    "/profile"
  ],
  "javaOnly": [
    "/portal/login",
    "/auth/set-password",
    "/clients/:id/theme",
    "/client-administration/users",
    "/identity/portal-users",
    "/identity/portal-apps",
    "/identity/developer-users",
    "/authentication/reset-approvals/:id?",
    "/platform/docs/:source?/:slug?",
    "/platform/settings/names"
  ]
}
